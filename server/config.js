// ─── SECRET ROOMS ────────────────────────────────────
// Přidej sem libovolné místnosti s limitem účastníků (overlay se nepočítá).
// Tyto místnosti nikdy nevyprší.
const SECRET_ROOMS = {
  'ACOBS26': { maxPeers: 4 }
};

module.exports = { SECRET_ROOMS };
